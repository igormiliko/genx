import MessageBrokerTest from "./MessageBrokerTest"

let MB = new MessageBrokerTest()

describe.only('MessageBroker pub/sub engine tests', () => {
    beforeAll(() => { 
        // Add new subscribers
        // Add new publishers
        // Initiate the publishers and the subscribers servers
    })

    test.only('main funtionality', () => {
        
    })
})